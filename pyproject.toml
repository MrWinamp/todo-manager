[project]
name = "todo-manager"
version = "0.0.1"
description = "Simple FastAPI TODO task manager"
requires-python = ">=3.10"
license = "Apache-2.0"
dependencies = [
    "fastapi",
    "uvicorn[standard]"
]
